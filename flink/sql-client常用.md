1. 常用sql

   ```sql
   show catalogs;
   use catalog myhive;
   show databases;
   use test;
   show tables;
   set table.sql-dialect=hive; -- to use hive dialect[设置成hive方言]
   set table.sql-dialect=default; -- to use default dialect[恢复默认方言]
   
   ```

   